import {describe, it} from 'vitest'
import {calc_cart_total} from './main'

describe("shopping cart total price calculation", () => {
    it("gives the price item if 1 item with amount 1 is in cart", () => {
    })
})